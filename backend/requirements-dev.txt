# Development & testing dependencies. Install: pip install -r requirements-dev.txt
-r requirements.txt

# --- Testing ---
pytest~=8.3
pytest-asyncio~=0.24
pytest-cov~=6.0
httpx~=0.27                     # FastAPI TestClient / async API tests
asgi-lifespan~=2.1             # exercise lifespan in tests

# --- Lint / format / types ---
ruff~=0.7
black~=24.10
mypy~=1.13

# --- Tooling ---
pre-commit~=4.0
